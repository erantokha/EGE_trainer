# W13.1 — Каталог части 2: класс/метод как измерения + 3-уровневый аккордеон + показ задачи

**RED-ZONE** (каталог-схема/RPC, `picker.js`). Контракт и решения —
`docs/navigation/part2_integration_contract.md`; схема контента — `docs/navigation/part2_content_schema.md`;
разведка — `reports/part2_recon/RECON.md`. Вход: данные из **W13.0** (готовые JSON части 2 + SVG).

## §1. Цель
Сделать №13 **видимым и навигируемым** на главной (ученик и учитель): после №12 с отступом, как
**3-уровневый аккордеон** (№13 «Уравнение» → класс → метод), с режимом «уникальные прототипы»; по нажатию
— показ задачи с условием и **эталоном** (пошаговое решение + окружность). **Метод = подтема**
(существующий `catalog_subtopic_dim`, как подтемы части 1 → статистика по методу собирается существующими
роллапами бесплатно); **класс** (триг/показ/лог) — визуальный заголовок-группировщик во фронте (тег в
данных, НЕ новое измерение каталога). Решение оператора 2026-06-18. Баллы/запись попытки — **НЕ здесь**
(это W13.2): задача пока показывается и решается «вхолостую» (эталон по кнопке), без зачёта.

## §2. Контекст
Каталог сейчас 2-уровневый (`renderSectionNode` L1 / `renderTopicRow` L2 в `picker.js`), данные —
backend-измерения `catalog_*_dim` через RPC `catalog_*_v1`, исходник `content/tasks/index.json`. №1..12
сортируются `sort_order`+`compareId()`. 3-уровневого моста L1→L2→L3 нет. `unic`-механизм
(`catalog_subtopic_unics_v1`, `#protoPickerModal`, `unique.html`) — готов. `picker.js` — активный трек W2.

## §3. Out of scope
- НЕ scoring / запись попытки / градусник / ДЗ — это W13.2.
- НЕ двухуровневая проверка (учительский балл).
- НЕ менять рендер/поведение части 1 (№1..12).
- НЕ рефакторить `picker.js` за рамками добавления 3-го уровня (трек W2 ведёт декомпозицию отдельно).

## §4. Затрагиваемые файлы (уточняется recon-шагом §5.2)
- `content/tasks/index.json` (+ возможно новые/расширенные `catalog_*_dim` и RPC `catalog_*_v1`).
- `tasks/picker.js` (рендер аккордеона: мост к 3-му уровню), `tasks/picker_*` модули.
- `pages/home-student.css` / `pages/home-teacher.css` (отступ после №12, стили уровней).
- `tasks/trainer.js` (показ stem + эталон: цепочка LaTeX + `<img>` окружность; режим «показать решение»).
- SQL-источники каталога в `docs/supabase/*` (если измерения уходят в бэкенд).

## §5. Пошаговый план
> **Task-tracking (обязательно, `CURATOR.md §6.1`):** TaskList по §5.1–§5.N, обновлять статусы.

- **§5.1** Прочитать контракт/схему/recon. Зафиксировать целевую структуру каталога части 2.
- **§5.2 RECON-уточнение (read-only, внутри волны):** подтвердить выполнимость подхода «метод = подтема»:
  как `index.json`/`catalog_subtopic_dim` представляют group→subtopic (вписать №13 группой + методы-подтемы
  **без новой схемы**); работает ли `unic`-механизм на уровне метода-подтемы; где в `picker.js` врезать
  фронт-группировку подтем по классу (заголовки триг/показ/лог); как роллапы (`student_topic_state_v1` +
  teacher-роллапы) подхватят новые подтемы. **Вывод — рекомендация куратору; stop-ask, если выяснится,
  что метод НЕ ложится на subtopic-dim как данные и потребует схемы/RPC.**
- **§5.3** Каталог-метаданные (**данные, не схема**). id-схема подтем (зафикс. 2026-06-18):
  `13.<class>.<method>` для триг (`13.trig.factor/quad/group/homog/other`), `13.<class>` для листьев
  (`13.log`/`13.exp`) — совпадает со слагами proto-id из W13.0. Внести №13 группой после №12 + 7
  методов-подтем в `index.json` (`catalog_subtopic_dim`), порядок методов через `sort_order`; класс
  выводится из id-префикса во фронте. **Манифест каждой подтемы: `topic`/`types[].id` = subtopic_id**
  (чтобы каталог, unic и `question_bank` слотировались по одному id). Деплой-шаги оператора:
  (1) `catalog_upsert_v1`; (2) **`tools/export_question_bank.mjs` + заливка `question_bank`** — иначе
  teacher-роллапы №13 не увидят (recon §5). Прогнать `tools/check_runtime_catalog_reads.mjs` +
  `check_runtime_rpc_registry.mjs`.
- **§5.4** Рендер: визуальная группировка подтем по классу в `picker.js` — **ветка `sec.id==="13"`** в
  `renderSectionNode` (`picker.js:2893`, плоский цикл по `sec.topics` на 2929): класс из id-префикса
  подтемы + явный массив порядка классов (триг→лог→показ). №1..12 идут прежним путём (ветка не
  затрагивается → регрессионно-безопасно). Отступ после №12 (CSS), на обеих главных. Лог/показ —
  класс-заголовок над одной листовой подтемой.
- **§5.5** Режим «уникальные прототипы» для части 2 (переиспользовать `unic`).
- **§5.6** Показ задачи: stem (а/б) через MathJax; кнопка «показать эталон» → `solution.steps` (LaTeX) +
  `gen_groups` + `below` + `<img>` окружность; ответ. Самопроверка-плейсхолдер (балл — в W13.2).
- **§5.7** Регрессия части 1: №1..12 рендер/подбор/каталог без изменений (визуальная + governance-проверка).
- **§5.8** Evidence: скриншоты аккордеона (ученик+учитель), открытой задачи №13 с эталоном; вывод governance.

## §6. Данные / контракты / миграции
Каталог-измерения класс/метод — **red-zone**, миграции схемы/RPC согласовать с куратором (stop-ask) и
со спеками `catalog_*`. Тела задач — формат `part2_content_schema.md` (из W13.0). Любая SQL — через
`docs/supabase/*` + `runtime_rpc_registry`.

## §7. Риски и stop-ask
- **RED-ZONE:** каталог-схема/RPC (`CURATOR.md §6.2`) — explicit approval, узкий scope, усиленное
  evidence; **stop-ask перед любой миграцией схемы/RPC**.
- `picker.js` — активный трек W2: координировать с куратором, не конфликтовать с декомпозицией;
  изменения минимальные и аддитивные (добавить L3, не переписывать L1/L2).
- Риск регрессии части 1 — §5.7 обязателен.
- Подход зафиксирован (2026-06-18): **метод = подтема** (бэкенд, для статистики), **класс = фронт-группировка**.
  Если recon §5.2 покажет, что метод НЕ ложится на существующий subtopic-dim как данные и требует новой
  схемы/RPC — stop-ask куратору (это меняет объём red-zone).

## §8. Autonomy policy (`CURATOR.md §6.3`)
Stop-ask-confirm обязателен при: изменении каталог-схемы/RPC/любой SQL-миграции (red-zone); касании
auth/RLS/scoring/ДЗ (вне scope/в W13.2); изменении рендера части 1; файлах вне §4. Свободно: порядок
§5, имена внутренних функций, CSS-детали отступа/уровней в пределах per-page. Поведение части 1 не менять.
