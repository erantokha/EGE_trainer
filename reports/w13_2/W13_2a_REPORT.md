# W13.2a — официальная шкала 0-32→100 (фронт). Отчёт исполнителя

Дата: 2026-06-18. Первый слайс W13.2 (одобрено дробление a/b/c + отдельная таблица `part2_attempt_reviews`).
**Чистый фронт, без миграций, без §5.0, без блокеров.** build `2026-06-18-3-021245`. Деплой БД не требуется.
Дизайн всей волны — `reports/w13_2/W13_2_RECON_DESIGN.md`.

## Сделано (только `tasks/picker_stats.js`)
- `SECONDARY_BY_PRIMARY`: добавлены официальные строки **13-32** (плато 30/31/32→100). Строки 0-12 не тронуты.
- Введён `MAX_PRIMARY_TOTAL = 32`; клампы `Math.min(12,…)` → `Math.min(32,…)` в `secondaryFromPrimary`,
  `secondaryFromPrimaryExact`, `thermoColorByPrimary` (план §5.5).
- `updateScoreForecast`: прогноз суммируется по **per-task `max_primary`** (часть 1 = 1/задание; №13 = 2)
  через `maxPrimaryForSection(id)`; цикл расширен до секции «13». До появления балла части 2 (W13.2b/c) её
  секции дают вклад 0 (точности по части 2 нет) → **прогноз части 1 не меняется**; достижимый потолок растёт
  по мере заливки части 2.
- `updateScoreThermo` (заливка `p/12`) НЕ трогалась — план её не перечисляет; генерализация максимума термометра
  под балл части 2 — в W13.2b/c (когда балл реально заливает термометр). Перекалибровка цветовых полос >12 — там же.
- GOAL=70 оставлен (изменение цели — отдельное продуктовое решение, не в scope).

## Регрессия (gate) — ЗЕЛЁНО
- `node --check tasks/picker_stats.js` — OK.
- `reports/w13_2/_scale_check.mjs` (импортирует реальные функции): **часть 1 (0-12) байт-в-байт идентична**
  (secondaryFromPrimary/Exact + дробная интерполяция 11.5→67, 8.5→49 + цвета термометра 0-12); строки 13-32
  корректны; плато 30/32→100; кламп снят (p=20→86, p=20→86 не упирается в 70).
- `check_no_eval` — ok; `print-features.js` — **36/0**.

## НЕ сделано (следующие слайсы, требуют тебя)
- **W13.2b** (самооценка ученика): таблица `part2_attempt_reviews` (self_score/status) + student-write +
  UI ученika 0/1/2 + прогноз «самооценка». **Предусловие: залить W13.1 SQL** (live-smoke №13).
- **W13.2c** (учитель): §5.0 выгрузка (`reports/w13_2/extract_prod_gates.sql` — **выполняешь ты в проде**) →
  teacher-write RPC + гейт + скоуп + аудит + RLS-негатив + read-side official (`teacher_score`) + прогноз
  «подтверждённый» + UI учителя в `renderReviewCards`.

## Действия оператора (разблокировка b/c)
1. Выполнить `reports/w13_2/extract_prod_gates.sql` в Supabase, результат закоммитить в `docs/supabase/`
   (дефиниции гейтов + RLS + DDL `homework_attempts`/`answer_events`). stop-ask при расхождении репо↔прод.
2. Залить W13.1 SQL (`catalog_upsert_v1.sql` + `question_bank_upsert_v1.sql`) — предусловие live-приёмки части 2.
