# W13.3 — полировка №13 (печать + бейдж + косметика). Отчёт

Дата 2026-06-18. **Frontend-only, без SQL/red-zone, автономно.** build `2026-06-18-9-190214`. Не закоммичено.
План `W13_3_PLAN.md`.

## §5.2 Печать №13 (`tasks/trainer/print.css`)
- Условие а/б печатается (как на экране, `.q-line`).
- Интерактив части 2 (`.part2-box` = тоггл+эталон, `.part2-selfscore`) — **скрыт в печати** (`display:none` в
  `@media print`); эталон по умолчанию **НЕ печатается** (модель «решает на бумаге»).
- **Рабочее место** под решение: `.task-ans:has(.part2-box)::after { content:"Решение:"; min-height:9cm }`
  (`:has()` уже используется в этом print.css). Часть 1 не затронута (классов `.part2-*` у №1..12 нет).
- **Эмпирически подтверждено** (Playwright `emulateMedia('print')`): part2-box/selfscore `visible=false`,
  условие = 2 строки (а/б), workspace `::after`="Решение:". 
- **Отложено (флаг):** опция «печать с эталоном» (учительская копия) — требует print-диалог (`print_btn.js`)
  + force-expand лениво-рендеримого эталона; вынесено как follow-up (не блокирует студенческую печать на бумаге).

## §5.3 FE-гашение бейджа №13 (`tasks/picker.js`)
Часть 2 — ручная проверка, точность неприменима → 0% (из answer_events ДЗ-сабмита) не показываем:
- **Аккордеон-бейдж темы** (`applyDashboardHomeStats`): для `isPart2Id(tid)` — нейтральный «—» + tooltip
  «Часть 2 — ручная проверка».
- **Бейджи модалки/карточек** (`setModalStatsBadge`, центрально): если ближайшая карточка part-2
  (`data-type-id`/`data-qid` через `isPart2Id`) → «руч.» + tooltip, без %. Покрывает proto-модалку и
  added-task разом. Часть 1 бейджи — без изменений. **Только отображение** (answer_events/RPC не трогали —
  корень в будущей волне B).

## §5.4 Косметика
Текущий стиль эталона/самооценки функционален (подтверждён скриншотами W13.2b/c). Спекулятивных косметических
правок НЕ вносил — финальный визуал утверждает оператор по скриншот-ревью (план §8). Готов внести точечные
правки `part2.css` по конкретному фидбэку.

## §5.5/§5.6 Регрессия + governance
- **`tests/print-features.js` — 36/0** (часть 1 печать не дрогнула; критично — трогал print.css).
- `node --check tasks/picker.js` — OK; governance (rpc 54/54, catalog, no_eval) — ok; часть 1 scale байт-в-байт.
- Бейджи/печать №1..12 — без изменений (ветки строго гейтятся `isPart2Id`/`.part2-*`).

## Out of scope (соблюдено)
Корневой фикс answer_events/score-семантики + статистика по методу = **отдельная red-zone-волна B (§5.0)** —
НЕ здесь. SQL/RPC/analytics не трогались.

## Накопленный незакоммиченный фронт
W13.1-fix + W13.2a/b/c/d/e + W13.3 — всё не запушено; для live-теста нужен commit+push (оператор).
