# Часть 2 (№13) — карта документации и передача в другие чаты

Как передавать работу исполнителям в **новых чатах** (без контекста этого обсуждения). Всё знание
вынесено в файлы ниже — execution от текущего диалога не зависит.

## Что где лежит
| файл | что это | формат |
|---|---|---|
| `docs/navigation/part2_integration_contract.md` | **SoT трека**: продуктовая модель, структура каталога, классификация (раскладка 75 клонов), 6 ключевых решений, последовательность, red-zone, открытые вопросы | контракт/решения |
| `docs/navigation/part2_content_schema.md` | схема полей задачи части 2 + спека конвертера (вход/выход/DoD) | контракт + спека |
| `reports/part2_recon/RECON.md` | разведка тренажёра по 7 подсистемам (`file:line`, как работает, точки встройки) | отчёт |
| `W13_0_PLAN.md` | волна W13.0 — конвертер банк→`content/` (офлайн, не red-zone) | план волны |
| `W13_1_PLAN.md` | волна W13.1 — каталог/3-уровневый аккордеон/показ (red-zone) | план волны |
| `W13_2_PLAN.md` | волна W13.2 — баллы + двухуровневая проверка (red-zone, тяжёлая) | план волны |
| банк: `~/Desktop/Вторая часть/13/` | 75 клонов, движок `_сборка/_engine.py` + `_solver_*.py` + `_card.py`, `bank_13_solutions.html` | исходник контента |

Порядок исполнения: **W13.0 → W13.1 → W13.2** (см. контракт §«Последовательность»). W13.0 можно начинать
сразу и параллельно.

## Как передать каждую волну (вставить в новый чат)

### Хэндофф W13.0 — конвертер (исполнитель)
> Прими роль исполнителя. Репозиторий тренажёра `/Users/anton/Projects/EGE_trainer`, банк
> `~/Desktop/Вторая часть/13`. Сначала прочитай: `docs/navigation/part2_integration_contract.md`,
> `docs/navigation/part2_content_schema.md`, затем работай **строго по `W13_0_PLAN.md`** (создай TaskList
> по §5). Это офлайн контент-трек (не red-zone). Соблюдай §3 Out of scope и §8 Autonomy. На выходе —
> конвертер `_сборка/_export_trainer.py`, JSON+SVG в `reports/part2_content_draft/`, сводка верификации.
> Приёмку делает куратор.

### Хэндофф W13.1 — каталог/показ части 2 (исполнитель). Остаток: §5.3–§5.8
> Прими роль исполнителя. Репозиторий `/Users/anton/Projects/EGE_trainer`. Прочитай:
> `docs/navigation/part2_integration_contract.md` (особенно раздел «Зафиксированные решения оператора»),
> `docs/navigation/part2_content_schema.md`, `reports/part2_recon/W13_1_RECON_5_2.md` (recon §5.2 —
> ВЫПОЛНЕН, GREEN: точки врезки, выводы), затем работай **по `W13_1_PLAN.md`, начиная с §5.3** (TaskList
> по §5.3–§5.8). **§5.1/§5.2 (recon) и W13.0 уже сделаны — НЕ переделывай.**
>
> **Подход зафиксирован — НЕ переоткрывай:** метод = подтема в существующем `catalog_subtopic_dim` (как
> подтемы части 1); класс (триг/лог/показ) = фронт-группировка в `picker.js`. **Новой каталог-схемы/RPC
> НЕТ — каталог растёт только данными.** id-схема подтем: `13.<class>.<method>` (триг:
> `13.trig.factor/quad/group/homog/other`), `13.<class>` для листьев (`13.log`/`13.exp`).
>
> **Остаток:**
> - **§5.3** (данные): №13 группой после №12 + 7 методов-подтем в `content/tasks/index.json`; **сплит**
>   `reports/part2_content_draft/part2_13.json` на 7 манифестов по подтеме (`topic`/`types[].id` =
>   subtopic_id), перенос в `content/` + SVG из `reports/part2_content_draft/img/part2/`; `sort_order`
>   для порядка методов; `"unic":true` на один клон из тройки.
> - **§5.4** (фронт): ветка `sec.id==="13"` в `renderSectionNode` (`tasks/picker.js:2893`, плоский цикл на
>   2929) — группировка подтем по классу из id-префикса + явный порядок классов (триг→лог→показ); отступ
>   после №12 (CSS). №1..12 НЕ трогать.
> - **§5.5** unic для №13 (механизм готов, нужны только теги в манифестах).
> - **§5.6** показ задачи: stem (а/б) через MathJax + кнопка «показать эталон» → `solution.steps`/
>   `gen_groups`/`below` + окружность `<img>` + `answer` (новый UI-блок в `tasks/trainer.js`).
> - **§5.7/§5.8** регрессия части 1 (№1..12 без изменений) + governance зелёные + evidence (скриншоты
>   аккордеона обе главные + задача №13 с эталоном).
>
> **Деплой БД — под оператора, НЕ считай сделанным:** (1) `catalog_upsert_v1` после правки `index.json`;
> (2) `tools/export_question_bank.mjs` + заливка `question_bank` (иначе teacher-статистика №13 пустая).
> Подготовь данные/команды, stop-ask перед допущением, что прод залит.
>
> **НЕ делать (это W13.2):** баллы/градусник/scoring/двухуровневая проверка/teacher-write. №13 пока
> решается «вхолостую» (эталон по кнопке). Соблюдай §3 Out of scope и §8 Autonomy. Приёмку ведёт куратор.

### Хэндофф W13.2 — баллы + двухуровневая проверка (исполнитель; red-zone)
> Прими роль исполнителя. Репозиторий `/Users/anton/Projects/EGE_trainer`. Предусловие: W13.0 и W13.1
> закрыты. Прочитай: `docs/navigation/part2_integration_contract.md`, `reports/part2_recon/RECON.md`,
> `docs/navigation/student_analytics_screen_v1_spec.md`, `docs/navigation/stage9_*` (write-path), затем
> работай **по `W13_2_PLAN.md`** (TaskList по §5). Это **RED-ZONE по нескольким осям** (scoring,
> write-path, RLS, teacher-доступ). Начни с recon-шага §5.2; **stop-ask перед каждой SQL/RLS/RPC-миграцией**;
> RLS-негатив-тесты обязательны (учитель не своего ученика — отказ). Часть 1 scoring не регрессировать.

### Хэндофф куратору (планирование/ревью волн)
> Прими роль куратора. Прочитай `CURATOR.md`, затем `docs/navigation/part2_integration_contract.md` и
> план соответствующей волны (`W13_1_PLAN.md` / `W13_2_PLAN.md`). Веди приёмку red-zone волн части 2,
> синхронизируй `GLOBAL_PLAN.md` (добавить трек W13) и прогресс-таблицу.

## Что ещё стоит сделать куратору (не сделано намеренно — оператор просил не передавать)
- Завести трек **W13** в `GLOBAL_PLAN.md` (строка в roadmap + прогресс-таблица по `CURATOR.md §2`).
- Решить открытые вопросы из контракта §«Открытые вопросы» при планировании каждой волны.
